import { LearnSegmentModel } from './learn-segment-model';

describe('LearnSegmentModel', () => {
  it('should create an instance', () => {
    expect(new LearnSegmentModel("TestArg")).toBeTruthy();
  });
});
